# AGENTS.md

## 목적

이 저장소는 **Apps in Toss(앱인토스)** 배포를 전제로 개발, 빌드, 배포 흐름을 정리하는 하니스다.
핵심 목적은 웹이든 게임이든 비게임이든, 사용자가 만들고자 하는 결과물을 **Apps in Toss에 실제로 배포 가능한 형태로 수렴**시키는 것이다.
공개 문서와 설명에서 저장소의 1차 정체성은 특정 상세 기술 스택이 아니라 **Apps in Toss 배포 하니스**다.
현재 코드베이스의 기본 실행 baseline은 RN reference implementation이지만, 저장소 목적 자체를 RN 전용 제품 저장소로 축소 해석하지 않는다.
현재는 기획 문서와 `PRD.md`를 만들지 않는다. 기능 범위는 사용자의 직접 지시와 공식 문서로만 확정한다.

## Source of Truth

우선순위는 아래 순서를 따른다.

1. 로컬 정리 문서: `docs/toss/integration/*.md`, `docs/setup/*.md`, `docs/ops/*.md`
2. 저장소 내 커스텀 스킬 정의: `skills/*/SKILL.md`
3. 로컬 기준 해시: `docs/toss/official-snapshots/manifest.json`
4. 공식 원문: `developers-apps-in-toss.toss.im`
5. 필요한 경우 `docs-search` 스킬로 공식 문서 재조회

규칙이 애매하거나 문서가 충돌하면 추측하지 말고 공식 문서를 다시 확인한다.
Apps in Toss 동작 방식, SDK 제약, 검수 기준은 관성적으로 일반 웹/모바일 앱 규칙을 가져오지 않는다.
백엔드, 결제, 로그인, 메시지, 프로모션 같은 서버 연동 이슈는 정리 문서를 입구로 보고 반드시 공식 원문으로 다시 맞춘다.
공개 저장소에는 공식 문서 원문 미러를 커밋하지 않는다. 원문이 필요하면 `docs-search` 또는 `npm run docs:sync`로 로컬에서만 가져온다.

## 커스텀 스킬 관리 규칙

- 이 저장소의 커스텀 스킬 원본은 반드시 `skills/` 아래에 둔다.
- 앞으로 추가되는 저장소 전용 스킬도 **반드시 repo 안의 `skills/<skill-name>/SKILL.md`로 버전관리**한다.
- 로컬 `~/.codex/skills`에만 두는 방식은 허용하지 않는다. 로컬 설치가 필요하더라도 **repo 안 원본이 먼저** 있어야 한다.
- 저장소 전용 스킬이 필요하면, 먼저 `skills/`에 원본을 추가하고 그 다음에 `AGENTS.md`에서 언제 읽어야 하는지 규칙으로 연결한다.
- Codex가 전역 스킬로 자동 인식하지 않더라도, 이 저장소에서는 필요한 경우 해당 `skills/*/SKILL.md`를 직접 읽고 그 절차를 따른다.

## 저장소 스킬 트리거 맵

다음 스킬은 아래 상황에서 우선적으로 읽는다.

- `skills/apps-in-toss-request-validator/SKILL.md`
  - 사용자의 요청이 Apps in Toss에 배포 가능한지 애매할 때
  - 정책/플랫폼 적합성부터 먼저 판별해야 할 때
- `skills/apps-in-toss-review-risk-checker/SKILL.md`
  - 기능은 가능해 보여도 검수/출시 반려 리스크가 걱정될 때
  - 외부 링크, 보상, 광고, 금융성, 브랜드/앱 분리 전략이 나올 때
- `skills/apps-in-toss-feature-prereq-mapper/SKILL.md`
  - 요청 기능의 선행 조건이 불명확할 때
  - 권한, 콘솔 설정, 앱 내 기능, 서버, mTLS, CORS, 수동 QA 여부를 빨리 정리해야 할 때
- `skills/apps-in-toss-backend-scope-slicer/SKILL.md`
  - 로그인, 결제, 인앱결제, 프로모션, 스마트 메시지, 서버 API 연동이 들어갈 때
  - 백엔드가 필요한지와 최소 범위를 잘라야 할 때
- `skills/apps-in-toss-manual-test-scribe/SKILL.md`
  - 샌드박스/실기기/QR/브리지/권한/결제처럼 자동화만으로 끝낼 수 없는 기능을 검증할 때
- `skills/apps-in-toss-release-readiness-checker/SKILL.md`
  - `.ait` 빌드 직전
  - 테스트 업로드 직전
  - 검토 요청 직전
  - 출시 직전

여러 스킬이 동시에 필요하면 아래 순서를 기본으로 한다.

1. `apps-in-toss-request-validator`
2. `apps-in-toss-review-risk-checker`
3. `apps-in-toss-feature-prereq-mapper`
4. `apps-in-toss-backend-scope-slicer`
5. 구현/테스트
6. `apps-in-toss-manual-test-scribe`
7. `apps-in-toss-release-readiness-checker`

## 현재 프로젝트 의사결정

- 이 하니스는 Apps in Toss에 배포 가능한 앱을 만드는 기반이어야 한다.
- 현재 코드베이스의 기본 reference 구현은 **React Native + Granite**다.
- 다만 저장소 목적은 특정 상세 스택 자체가 아니라 **Apps in Toss 배포 가능성 확보**다.
- 사용자가 WebView, 게임, 비게임 등 다른 방향을 요구하면, 먼저 Apps in Toss 제약과 검수 기준으로 유효성을 판단한 뒤 범위를 조정한다.
- 개발 방법론은 **TDD(Test-Driven Development)** 로 고정한다.
- 제품 요구사항은 아직 고정하지 않는다. 기능 제안이나 화면 설계는 요청이 있을 때만 한다.
- `PRD.md`, 상세 기획서, 정책 문서는 별도 요청 전까지 만들지 않는다.
- 구현 중 Apps in Toss 제약이 제품 요구와 충돌하면, 먼저 제약을 문서 근거와 함께 드러내고 범위를 조정한다.
- 저장소 변경은 특정 앱의 비즈니스 로직을 하드코딩하는 방향보다, **하니스 자체의 재사용 가능한 기능/문서/검증 흐름을 강화하는 방향**을 우선한다.

## 공통 개발 원칙

- **SDK 2.x만 사용한다.** 공식 문서 기준 `2026-03-23` 이후에는 SDK 1.x로 빌드한 번들을 콘솔에 업로드할 수 없다.
- 이 저장소는 RN 기준으로 유지한다. WebView 전환이 필요하면 먼저 이유와 마이그레이션 범위를 명시한다.
- `appName`은 Apps in Toss 콘솔에 등록한 이름과 **완전히 동일**해야 한다.
- 새 앱 이름을 정해야 하면 `kebab-case`를 사용한다.
- `appName`은 딥링크(`intoss://{appName}`), 테스트, 배포 주소에 모두 쓰이는 **안정 식별자**이므로 함부로 바꾸지 않는다.
- 권한이 필요한 기능은 실제 사용 범위만 최소로 선언한다. 선언은 `granite.config.ts`에서 관리한다.
- 비게임 앱은 TDS 사용이 **필수**다. TDS를 빼고 UI를 임의 구현하는 방향은 기본 선택지가 아니다.
- `iframe`은 사용할 수 없다. YouTube 영상 삽입 용도만 예외로 본다.

## 프레임워크별 규칙

### React Native

- 패키지는 `@apps-in-toss/framework`를 사용한다.
- 기존 RN 앱이 있어도 Apps in Toss 배포 대상이면 **Granite 기반으로 스캐폴딩**한다.
- `ait init`으로 `granite.config.ts`를 구성한다.
- 로컬 개발은 `granite dev`, 패키징은 `ait build`, 업로드는 `ait deploy` 기준으로 맞춘다.
- 비게임 RN 앱은 TDS 패키지 사용을 **필수**로 본다.
  - `@apps-in-toss/framework >= 1.0.0` 이면 `@toss/tds-react-native`
  - 그 미만은 구 패키지 계열 문서를 재확인한다.

## 인증, 네트워크, 보안 규칙

- 라이브 환경은 **HTTPS만 지원**한다. 샌드박스에서 HTTP가 되더라도 운영 환경 기준으로 설계한다.
- CORS 허용 목록에는 최소한 아래 Origin을 반영한다.
  - `https://<appName>.apps.tossmini.com`
  - `https://<appName>.private-apps.tossmini.com`
- iOS/iPadOS 13.4+의 서드파티 쿠키 차단 때문에, 파트너사 도메인에서 **쿠키 기반 로그인**에 의존하지 않는다.
- 인증은 가능하면 **토큰 기반**으로 설계한다.
- Apps in Toss API를 서버에서 호출할 때는 **mTLS**를 전제로 설계한다.
- 비밀키, 토큰, 승인용 시크릿은 클라이언트 번들에 넣지 않는다.

## 서버 API 연동 규칙

- Apps in Toss 서버 API는 **파트너사 서버에서 호출**한다. 클라이언트가 직접 `apps-in-toss-api.toss.im`, `pay-apps-in-toss-api.toss.im`를 호출하는 구조를 기본안으로 두지 않는다.
- mTLS 인증서와 키 파일은 콘솔에서 발급받아 **안전한 위치**에 보관한다. 저장소, 클라이언트 번들, 공개 CI 아티팩트에 포함하지 않는다.
- 인증서는 **만료 전에 재발급**하는 것을 운영 체크리스트에 넣고, 가능하면 **다중 인증서 등록**을 전제로 무중단 교체가 가능하게 설계한다.
- 방화벽을 관리하는 인프라라면 Apps in Toss 문서의 **Inbound / Outbound IP와 443 포트 허용**을 배포 전 체크리스트에 포함한다.
- 서버 API 응답은 항상 `resultType`을 먼저 검사한 뒤 `success`와 `error`를 분기한다.
- 서버 API 기본 한도는 **분당 3,000 QPM**으로 본다. 예상 트래픽이 이를 넘길 가능성이 있으면 오픈 전에 상향 협의를 전제로 설계한다.

## 권한 규칙

- 권한은 `granite.config.ts`에 선언하고, 검수 대상 설정으로 취급한다.
- 현재 공식 문서에서 확인한 권한 이름:
  - `clipboard`
  - `contacts`
  - `photos`
  - `camera`
  - `geolocation`
- WebView에서도 권한 선언 방식은 동일하다.
- 권한이 필요한 기능을 추가할 때는 UX 문구, 실패 처리, 권한 거부 시 대체 흐름까지 함께 설계한다.

## 테스트와 배포 규칙

- 새 기능과 버그 수정은 **테스트 먼저 작성**하는 것을 기본 규칙으로 한다.
- 구현 순서는 가능한 한 **Red → Green → Refactor** 를 따른다.
- 버그 수정은 재현 테스트 또는 회귀 테스트를 먼저 추가한 뒤 수정한다.
- 테스트 없이 구현만 먼저 넣는 방식은 예외 사유가 분명할 때만 허용한다.
- 개발과 QA는 **샌드박스 앱**을 기본 경로로 사용한다.
- Apps in Toss는 별도 개발용 토스앱을 제공하지 않는다.
- RN 0.84 / SDK 2.x 테스트는 공식 문서 기준 **`2026-03-06` 업로드 버전 이상의 최신 샌드박스 앱**에서 진행한다.
- 실서비스 출시 전에는 샌드박스 앱에서 기능 검증을 완료해야 한다.
- 샌드박스에서 통과해도 검수 가이드 위반이면 반려될 수 있다.
- iOS 실기기 샌드박스 테스트는 **같은 Wi-Fi 연결**과 **로컬 네트워크 권한 허용**을 전제로 한다.
- 제출 산출물은 `.ait` 번들이다.
- 업로드 가능한 앱 번들은 **압축 해제 기준 100MB 이하**여야 한다.
- 이미지, 사운드, 영상 등 대용량 리소스는 번들에 몰아넣지 말고 CDN/외부 스토리지/지연 로딩을 우선 검토한다.
- 콘솔에서 **테스트를 1회 이상 완료한 뒤에만** 검토 요청을 진행한다.
- 검토는 공식 문서 기준 **영업일 최대 3일**까지 소요될 수 있다.
- 검토 요청은 **한 번에 하나의 버전만** 가능하다.
- 버그를 고치려면 검토를 취소하고 새 `.ait`를 업로드한 뒤 다시 요청한다.
- 출시 버튼을 누르면 **즉시 전체 사용자에게 반영**되므로 라이브 차이까지 확인한 뒤 출시한다.
- CI/CD 자동화는 기본적으로 **테스트 업로드까지**로 본다.
- `검토 요청`, `승인`, `출시하기`는 공식 문서 기준 **콘솔 수동 게이트**로 유지한다.
- 라이브 직전 점검 항목:
  - CORS
  - HTTPS 강제
  - 권한 처리
  - 로그인/세션 유지
  - 실제 결제/인증
  - 외부 리소스 로딩
- 출시 후에는 오류 로그, 크래시, API 지연, CDN 실패, 사용자 피드백을 모니터링한다.

## 로그인/사용자 정보 규칙

- `authorizationCode`는 **10분**, `accessToken`은 **1시간**, `refreshToken`은 **14일** 유효하다는 전제로 만료/재발급 흐름을 구현한다.
- `authorizationCode`는 1회성으로 취급한다. 만료되었거나 같은 코드로 중복 토큰 발급을 시도하는 흐름을 정상 시나리오로 가정하지 않는다.
- `login-me`의 개인정보는 **암호화된 형태**로 내려오므로 복호화는 서버에서 처리한다.
- 복호화 키와 AAD는 운영 시크릿으로 관리한다. 클라이언트나 테스트 샘플 코드에 하드코딩하지 않는다.
- `scope`는 앞으로 새 값이 추가될 수 있으므로 **미지의 scope를 허용하는 방식**으로 파싱한다.
- 로그인 연결 해제 콜백을 쓰는 경우, 파트너 서버는 **GET / POST 둘 다 처리 가능하게** 두는 것을 기본으로 본다.
- 로그인 연결 해제 콜백을 쓰는 경우, 콘솔에 설정한 **Basic Auth 헤더 검증 방식**까지 포함해 운영한다.
- 서비스에서 직접 로그인 끊기 API를 호출한 경우에는 **콜백이 오지 않는다**는 점을 전제로 상태 동기화를 설계한다.
- `remove-by-user-key` 호출 시 `readTimeout(3초)`가 날 수 있으므로, 문서 기준으로 **즉시 재시도하지 않고 일정 시간 후 재시도**한다.

## 결제와 주문 규칙

- `checkoutPayment`는 사용자 인증만 수행한다. **실제 결제 실행은 서버에서** `execute-payment`로 처리한다.
- `orderNo`는 가맹점 기준 **매회 유니크**해야 한다.
- `orderNo`는 허용 문자와 길이 제한을 지키고, **구매자 인증 완료 이후 재사용하지 않는다**.
- 테스트/라이브 환경 간 `orderNo` 충돌이 없도록 주문번호 전략을 분리한다.
- `payToken`은 매회 유니크하므로 서버에서 **반드시 저장**하고, 결제/환불/상태조회의 기준값으로 쓴다.
- `transactionId`는 대사와 추적용 식별자로 저장한다.
- `isTestPayment`는 발급된 `payToken`의 환경과 일치해야 한다.
- 승인/환불 응답을 받지 못한 경우를 대비해 **결제 상태 조회 API를 정합성 복구 경로**로 설계한다.
- 토스머니 결제는 **은행 점검 시간 오류**가 발생할 수 있으므로 사용자 안내와 재시도 흐름을 준비한다.

## 인앱결제 규칙

- 인앱결제는 **상품 지급 완료**까지 구현 범위에 포함한다. 결제 성공만으로 끝난 것으로 처리하지 않는다.
- 사용자가 기기를 바꿔도 지급 상태를 복원할 수 있도록 **저장소 + 토스 로그인 + 주문 상태 조회 API**를 함께 고려한다.
- `getPendingOrders`와 `completeProductGrant`를 이용한 **미결 주문 복원 흐름**을 필수 플로우로 본다.
- 샌드박스에서는 최소한 아래 3가지를 각각 검증한다.
  - 결제 성공
  - 결제 성공 + 파트너 서버 지급 실패
  - 오류 시나리오
- 샌드박스 상품 목록은 **콘솔에서 노출 ON인 상품만** 내려온다는 전제로 QA한다.
- 앱마켓 환불은 앱인토스가 보장하지 않으므로 문서 기준 테스트는 **소액으로만** 진행한다.

## 프로모션과 메시지 규칙

- 게임/비게임 프로모션 모두 **중복 지급 방어 로직**을 필수로 둔다.
- 실제 프로모션 시작 전에는 **테스트용 프로모션 코드로 최소 1회 이상 호출**해 승인/등록 상태를 확인한다.
- 자체 리워드 명칭에 `포인트`를 쓰거나, 현금화로 오인될 수 있는 `출금`, `인출` 같은 표현을 쓰지 않는다.
- 스마트 메시지 테스트 발송은 **사용자 인증 토큰**과 메시지 발송 **scope**를 전제로 한다.
- 스마트 메시지 실발송 API는 `x-toss-user-key`를 요구하므로, 로그인과 사용자 키 조회가 선행된다고 가정한다.

### TDD 적용 원칙

- 순수 로직, 상태 전이, 파서, 포맷터, 유틸 함수는 자동 테스트로 먼저 고정한다.
- 화면 개발도 가능한 범위에서는 렌더링/상호작용 테스트를 먼저 작성한다.
- Apps in Toss SDK, 샌드박스 앱, 토스앱, QR 테스트처럼 외부 런타임 의존성이 큰 영역은 테스트 대상을 분리한다.
  - 내부 로직은 자동 테스트로 보호한다.
  - 브리지 호출, 권한 팝업, 샌드박스/실기기 동작은 수동 검증 시나리오로 보완한다.
- 자동 테스트로 대체할 수 없는 항목은 PR 또는 작업 결과에 최소한 아래를 남긴다.
  - 무엇을 자동 테스트하지 못했는지
  - 왜 못했는지
  - 어떤 수동 검증 경로로 확인했는지

## 앱 내 기능 규칙

- **비게임 앱은 콘솔의 `앱 내 기능`을 최소 1개 이상 등록**해야 한다.
- 앱 내 기능 검토는 공식 문서 기준 **영업일 1~2일**이 추가로 걸릴 수 있다.
- 앱 내 기능은 사용자가 홈을 거치지 않고 특정 기능으로 바로 진입하는 경로다.
- 앱 내 기능은 `intoss://{appName}/...` 경로와 연결한다.
- 등록한 앱 내 기능은 **미니앱 내부에서 완결적으로 경험**할 수 있어야 한다.
- 핵심 플로우를 외부 링크로 넘기는 구조는 기본적으로 허용되지 않는다고 보고 설계한다.

## 정책과 검수 리스크

다음 범주는 기본적으로 출시 불가 또는 고위험으로 취급한다.

- 디지털 자산, 가상자산, NFT
- 현금 또는 유사 자산의 직접 교환/전환/환불 구조
- 불법, 해킹, 우회, 사행성, 복권, 베팅성 콘텐츠
- 금융상품 중개, 판매, 광고
- 투자 자문, 리딩방, 유료 정보 제공
- 기존 회사/서비스를 단순 홍보하는 용도의 미니앱

아래 행위도 기본 금지로 본다.

- 동일 워크스페이스에서 유사 기능 앱을 여러 개 쪼개 출시
- 동일 브랜드를 반복 노출하기 위한 다중 앱 운영
- 자사 앱 설치 유도
- 외부 링크에 핵심 플로우를 의존하는 구조

외부 링크는 법률상 고지, 공공기관/제휴기관 공식 페이지, 단순 정보 확인 같은 제한적 예외만 검토한다.

## 요청 유효성 판단 규칙

- 사용자의 요청을 받으면 구현 전에 먼저 **Apps in Toss 기준 유효성**을 판단한다.
- 판단 결과는 아래 셋 중 하나로 분류한다.
  - `허용`: 현재 문서와 정책 범위에서 구현 가능
  - `조건부`: 구현 가능하지만 권한, 서버, 심사, 정책, 운영 리스크가 큼
  - `불가/고위험`: 현재 정책상 금지되었거나, 검수 반려 가능성이 높거나, 플랫폼 구조와 충돌함
- `불가/고위험`이면 바로 구현하지 않는다. **왜 안 되는지**를 구체적 이유와 함께 설명하고, 가능하면 Apps in Toss 안에서 성립하는 대안을 제시한다.
- `조건부`면 필요한 선행 조건을 먼저 드러낸다.
  - 예: 서버 필요, mTLS 필요, HTTPS 필요, 권한 선언 필요, 콘솔 설정 필요, 앱 내 기능 필요, 심사 리스크 존재
- 설명은 추상적으로 하지 않는다. 다음 중 가능한 근거를 함께 붙인다.
  - `AGENTS.md`의 정책/검수 규칙
  - `docs/toss/integration/*.md`
  - `docs/setup/*.md`
  - 공식 문서 URL
- Apps in Toss에서 할 수 없는 것을 사용자가 원할 때는 **가능한 척 우회 구현을 제안하지 않는다.**
- 특히 아래 요청은 우선적으로 유효성 검토 대상으로 본다.
  - 외부 앱 설치 유도
  - 외부 링크에 핵심 플로우 의존
  - 금융상품 중개/광고/권유
  - 투자 자문, 리딩방, 유료 정보 제공
  - 디지털 자산, NFT, 가상자산
  - 현금화, 출금, 인출, 환불, 전환 구조
  - 사행성, 베팅, 복권성 기능
  - 검수 우회, 정책 우회, 샌드박스와 라이브 차이를 이용한 우회
  - 라이브에서 HTTP 사용
  - 쿠키 기반 로그인에 의존하는 구조
  - Apps in Toss 서버 API를 클라이언트에서 직접 호출하려는 구조
  - `iframe` 기반 구조

### 요청 유효성 응답 방식

- `허용`이면 구현 방향을 바로 이어간다.
- `조건부`면 아래 형식으로 먼저 설명한다.
  - 무엇이 가능한지
  - 무엇이 추가로 필요한지
  - 어떤 리스크가 있는지
  - 안전한 대안이 무엇인지
- `불가/고위험`이면 아래 형식으로 답한다.
  - 왜 Apps in Toss 기준으로 부적합한지
  - 어떤 정책/제약과 충돌하는지
  - 요구사항을 어떻게 바꾸면 배포 가능한 범위로 들어오는지
- 유효성 판단이 애매하면 구현보다 먼저 질문하거나 `docs-search`로 공식 문서를 다시 확인한다.

## 생성형 AI 기능 규칙

생성형 AI 기능을 넣는 경우 아래를 기본 요구사항으로 본다.

- 최초 사용 시 생성형 AI 사용 사실을 고지한다.
- AI가 만든 결과물은 사용자가 즉시 인지할 수 있게 표시한다.
- 법률/정책 리스크가 크므로, 실제 구현 전에 관련 공식 문서를 다시 확인한다.

## 기능별 예외 메모

- 인앱 광고는 **샌드박스 앱에서 테스트할 수 없다.** 공식 문서 기준 콘솔 QR 코드 경로로 테스트한다.
- 구독 기능은 공식 문서 기준 현재 **샌드박스 테스트를 지원하지 않으며**, **iOS에서는 이슈로 사용 지양** 안내가 있다.
- 광고, 결제, 구독, 로그인, 인증 같은 민감 기능은 구현 직전에 `docs-search`로 최신 문서를 다시 확인한다.

## 작업 방식

- Apps in Toss 전용 기능을 추가하거나 수정할 때는 먼저 `docs/toss/integration/*.md` 또는 `docs-search`로 근거 섹션을 찾는다.
- 사용자의 요청이 Apps in Toss에 배포 가능한지 애매하면, `skills/apps-in-toss-request-validator/SKILL.md`를 먼저 읽고 같은 절차로 유효성을 판별한다.
- 구현 전에 요청의 검수 리스크가 걱정되면, `skills/apps-in-toss-review-risk-checker/SKILL.md`로 한 번 더 분리해서 본다.
- 기능 요구가 구체적이지만 준비물이 불명확하면, `skills/apps-in-toss-feature-prereq-mapper/SKILL.md`로 선행 조건을 먼저 정리한다.
- 서버/API 연동 요구가 있으면, 구현 전에 `skills/apps-in-toss-backend-scope-slicer/SKILL.md`로 최소 백엔드 범위를 먼저 자른다.
- 자동 테스트만으로 충분하지 않은 기능이면, 구현 후 `skills/apps-in-toss-manual-test-scribe/SKILL.md`로 수동 검증 시나리오를 남긴다.
- 빌드, 테스트 업로드, 검토 요청, 출시 직전에는 `skills/apps-in-toss-release-readiness-checker/SKILL.md`로 게이트를 다시 확인한다.
- 업무별 API 범위를 빠르게 파악할 때는 `docs/toss/integration/*.md`를 입구로 쓰고, 실제 구현 전에는 반드시 공식 원문으로 다시 맞춘다.
- 서버 API나 백엔드 규칙을 건드릴 때는 `docs-search` 또는 공식 원문 URL을 먼저 확인한다.
- 로컬 문서만으로 불확실하면 `docs-search`를 사용해 공식 원문을 확인한다.
- 문서 최신화는 수동 업무로 미루지 않는다. `docs/toss/official-snapshots`와 GitHub Actions `Docs Freshness` 결과를 주기적으로 확인한다.
- 추적 중인 핵심 공식 문서가 변경되면 `AGENTS.md`, `STACK.md`, `docs/setup/apps-in-toss-rn.md`, `docs/toss/integration/*`를 같이 갱신한다.
- 공개 전에는 반드시 `npm run public:check`를 통과시킨다.
- 문서 근거 없이 플랫폼 동작을 추정하지 않는다.
- 일반 웹 관습보다 Apps in Toss 검수/배포 규칙을 우선한다.
- 구현 전에 먼저 테스트 가능 경계를 정하고, 그 경계부터 실패하는 테스트를 만든다.
- 테스트하기 어려운 코드는 그대로 밀어붙이지 말고, 테스트 가능한 구조로 분리하거나 어댑터 뒤로 숨긴다.
- 불확실한 요구사항은 임의로 제품 결정을 내리지 말고, 안전한 골격까지만 만들고 질문한다.
- 스택 선택과 운영 전제는 `STACK.md`, 세팅 절차는 `docs/setup/apps-in-toss-rn.md`에 계속 반영한다.

## 빠른 참고 링크

- 출시: `https://developers-apps-in-toss.toss.im/development/deploy.md`
- 비게임 출시 가이드: `https://developers-apps-in-toss.toss.im/checklist/app-nongame.md`
- 서비스 오픈 정책: `https://developers-apps-in-toss.toss.im/intro/guide.md`
- 샌드박스 앱: `https://developers-apps-in-toss.toss.im/development/test/sandbox`
- 앱 내 기능: `https://developers-apps-in-toss.toss.im/development/test/function.md`
- 권한 설정: `https://developers-apps-in-toss.toss.im/bedrock/reference/framework/권한/permission.md`
- React Native 시작: `https://developers-apps-in-toss.toss.im/tutorials/react-native.md`
- WebView 시작: `https://developers-apps-in-toss.toss.im/tutorials/webview.md`
